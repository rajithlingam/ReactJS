{
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "*": ["node_modules/*"]
    },
    "jsx": "react-jsx" // Use "react" if you're using React 16 or earlier
  },
  "include": ["src/**/*"],
  "exclude": ["node_modules", "dist"] // Optional: Add if needed
}